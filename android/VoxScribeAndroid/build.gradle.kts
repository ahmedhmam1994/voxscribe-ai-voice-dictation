// Top-level build file. Android Studio will offer to fix the Gradle/AGP/Kotlin
// versions here on first sync if newer ones are available -- accept that offer.
plugins {
    id("com.android.application") version "9.3.1" apply false
}
